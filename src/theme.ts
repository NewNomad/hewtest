import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    // TODO:テーマ設定を行います

    palette: {
        primary: {
            main: "#f69c2f"
        },
        secondary: {
            main: "#3879c3"
        },
        background: {
            default: "#fff"
        }
    }
});

export default theme;