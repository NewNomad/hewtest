export type TypeProducts = {
    id: number;             // 商品ID
    name: string;           // 商品名
    price: number;          // 価格
    isice: number;          // ホット/アイス
    stock: number;          // 在庫
    imageURL: string;       // 画像情報
    quantity:number;        // 選択個数
}